import 'package:flutter/material.dart';

class ShoeItemOurProductsWidget extends StatelessWidget {
  const ShoeItemOurProductsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
